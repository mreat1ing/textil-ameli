import { FC } from 'react';

import './Address.scss';

const Address: FC = () => {
  return (
    <p className="address">
      Севастополь, МЦ “DOMINO” (б-р Гидронавтов, 60, 2 эт.)
    </p>
  );
};

export default Address;
